extends layout

block head
    title Stops for #{routeNameAndDirection}

block body
    .container
        | #{ routeNameAndDirection }<br/><br/>
        | Choose your stop:<br/>
        each s in stops
            | <a class="clickable" href="/s/#{s.stopId}?rt=#{routeDirectionCode}">#{s.stopName}</a>
        | <br/><a href="/">Back to homepage</a>
